{
  "name": "blindference",
  "version": "0.1.0",
  "private": true,
  "description": "Blindference protocol — confidential inference network on Reineira (workspace root)",
  "license": "BUSL-1.1",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
